# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment and secrets
.env
.env.*
!.env.example

# Supabase CLI state
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# design-tool session cache (absolute local paths)
.impeccable/hook.cache.json

# Native build inputs supplied per-project, never committed (see docs/MOBILE.md)
android/app/google-services.json
ios/App/App/GoogleService-Info.plist

# @capacitor/assets writes a stray PWA icon set at the repo root; public/icons is the real one
/icons/
